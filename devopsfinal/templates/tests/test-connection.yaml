apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devopsfinal.fullname" . }}-test-connection"
  labels:
    {{- include "devopsfinal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devopsfinal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
