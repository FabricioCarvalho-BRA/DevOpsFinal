apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopsfinal.fullname" . }}
  labels:
    {{- include "devopsfinal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopsfinal.selectorLabels" . | nindent 4 }}
